## in: path to json file for single participant, single task
## intermediate: trials*featureIndices, stimuli*features
## out: trials*features

## fully array based:
"""
## fully array based:

trials x (max) features, dtype= boolean
trials x meta (object, response, etc), dataframe
objects x features x params (freq, ori, x, y)
objects x gabvects


PARAMS (6): size, freq, oris, phase, x, y;
"""
